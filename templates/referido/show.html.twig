{% extends 'base.html.twig' %}

{% block title %}Referido{% endblock %}

{% block body %}
    <h1>Referido</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ referido.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ referido.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('referido_index') }}">back to list</a>

    <a href="{{ path('referido_edit', {'id': referido.id}) }}">edit</a>

    {{ include('referido/_delete_form.html.twig') }}
{% endblock %}
